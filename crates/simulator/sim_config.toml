pool_address = "0xC6962004f452bE9203591991D15f6b388e09E8D0"
mongo_uri = "mongodb://localhost:27017"
db_name = "azoth_simulation"
fee = 500
tick_spacing = 10

genesis_block = 99174111
from_block = 110174111
to_block = 120174111

deposit_weth = "1000000000000000000"

# Token metadata
token0_decimals = 18
token1_decimals = 6
token0_symbol = "WETH"
token1_symbol = "USDC"

# ── Strategy parameters ──────────────────────────────────────────────────
# Wide range: ±wide_range_pct% from current price
wide_range_pct = 70
# Base range: ±base_range_pct% from current price
base_range_pct = 35
# Limit order range: single-sided, limit_order_pct% from current price
limit_order_pct = 10

# Capital allocation: wide gets wide_alloc%, base gets (100 - wide_alloc)%
wide_alloc_pct = 10

# Rebalance triggers (whichever hits first)
# Rebalance if price moves more than this % from last rebalance price
rebalance_price_pct = 20
# Rebalance every N blocks (~345600 blocks = 1 day on Arbitrum)
rebalance_interval_blocks = 345600

# Output
write_csv = false

